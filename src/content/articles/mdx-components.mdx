---
title: MDX Components Demo
date: 2026-08-26
category: NOTES
type: writing
---

This article exercises every MDX utility component available on this site.

<Callout type="tip">
  All components below are plain JSX written directly in this `.mdx` file.
</Callout>

## Callout

<Callout type="warning" title="Careful now">
  This will delete all your data. Make sure you have a backup.
</Callout>

<Callout type="danger">Do not run this against production.</Callout>

<Callout>Note: defaults to the info variant.</Callout>

## CodeBlock

<CodeBlock language="tsx" filename="src/components/Button.tsx" highlight="2">

```tsx
export function Button({ children }) {
  return <button className="btn">{children}</button>;
}
```

</CodeBlock>

## Steps

<Steps>

### Install dependencies

Run the following command to install the required packages.

```bash
bun install
```

### Write some MDX

Create a new `.mdx` file under `src/content/articles`.

### Build the site

Run the dev server and check the output.

</Steps>

## Accordion

<Accordion title="Why use Accordion?">
  Accordions let you hide lengthy explanations behind a click,
  keeping the article scannable while still providing depth
  for readers who want it.
</Accordion>
<Accordion title="Why use Accordion?">
  Accordions let you hide lengthy explanations behind a click,
  keeping the article scannable while still providing depth
  for readers who want it.
</Accordion>

## Tabs

<Tabs defaultValue="bun">

<Tab value="bun">

```bash
bun install
```

</Tab>

<Tab value="npm">

```bash
npm install
```

</Tab>

</Tabs>

## TOC

<TOC />
