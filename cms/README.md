# Local Payload CMS

Run `bun run dev -- --port 3001` from this directory, or run `sst dev` from the repository root.

Open `http://localhost:3001/admin` and create the first user. SQLite data is stored in the ignored `payload.db` file.
